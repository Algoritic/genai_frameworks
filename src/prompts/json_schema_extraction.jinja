You are a JSON Schema expert tasked with converting unstructured text into a VALID JSON Schema compliant with the 2020-12 draft (https://json-schema.org/draft/2020-12/json-schema-core).

Generate a JSON schema for the following document:
{{ocr_output}}

 The output should be a valid JSON schema that includes:
- Appropriate title and description
- Required fields
- Property definitions with types and descriptions

Schema:
