#Get user input
#Update Conversation History
#Fetch from knowledge base based on user input
#if conversation history == 1, skip conversation history summarization
#conversation history -> mem0
#create prompt via jinja template (user input + conversation history + knowledge base)
#llm(prompt)
