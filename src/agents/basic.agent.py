class BasicAgent:

    def __init__(self, system_prompt, user_prompt, llm):
        pass

    def generate_response(self, prompt):
        #context = query knowledge base with user input + conversation history (summarization of last k)
        #response = llm(prompt)
        pass
